Libraries placed in this folder are loaded into every Mimicrune chapter (for convenience). Use the
same layout as a project's `libraries` folder

If a chapter bundles a library with the same ID, the chapter's copy takes precedence over the shared copy.
