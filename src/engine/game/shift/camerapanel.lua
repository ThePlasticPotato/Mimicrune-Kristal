---Camera controlling variant of the generic ShiftPanel. Has camera buttons and a display of a ShiftCamera, masked to its surface.
---@class CameraPanel : ShiftPanel
local CameraPanel, super = Class(ShiftPanel)

return CameraPanel